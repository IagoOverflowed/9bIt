# The Heavycore Index (HC$)
Pricing references using a single heavy core
# Main ref: 1x Heavy core ITEM, not stack, not shulker, not dub, a.k.a Heavy Core, or simply, HCI
# Listings

1 HCI = 1 FD3